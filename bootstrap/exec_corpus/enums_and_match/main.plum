enum Shape { Circle(Int), Square(Int) }
let area (s: Shape): Int = match s {
    Circle(r) => r * r * 3,
    Square(side) => side * side,
}
let main (): Unit = {
    println(area(Circle(2)).to_string());
    println(area(Square(3)).to_string());
}
