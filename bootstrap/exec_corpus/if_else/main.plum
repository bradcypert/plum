let classify (n: Int): String = if n > 0 { "positive" } else { if n < 0 { "negative" } else { "zero" } }
let main (): Unit = {
    println(classify(5));
    println(classify(-5));
    println(classify(0));
}
